package !PACKAGE!;

import com.xross.tools.xdecision.DecisionTree;
import com.xross.tools.xdecision.utils.DecisionTreeFactory;

public class !TEST_CLASS! {
	public static void main(String[] args){
		DecisionTree<String> tree = new DecisionTreeFactory().createFromXML("!MODEL_PATH!");
		
		//Verify tree
		Object[] test = new Object[!FACTOR_COUNT!];
		!TREE_VERIFY!
	}
	
	private static String convert(Object[] test){
		StringBuffer valueBuf = new StringBuffer();
		for(int i = 0; i < test.length; i++)
			if(test[i] == null)
				valueBuf.append("[]");
			else
				valueBuf.append("[" + test[i] + "]");
		return valueBuf.toString();
	}
	
	private static void assertEquals(Object[] test, String expected, String actual){
		System.out.println("Get: " + convert(test));
		System.out.println("Expected: " + expected + (expected.equals(actual)? "": "not") + " equals to actual: " + actual);
		System.out.println((expected.equals(actual)? "O": "X"));
		System.out.println();
	}
}
